import React from 'react';
import styled from 'styled-components';

const WorksitesWrapper = styled.div`
  display: flex;
  flex: 1 1 auto;
  justify-content: center;
`;

const Worksites = () => (
  <WorksitesWrapper />
);

export default Worksites;
