import React from 'react';

import { BackNavButton } from '../controls/index';
import { FixedWidthWrapper } from '../Layout';

const ParticipantProfile = () => (
  <FixedWidthWrapper>
    <BackNavButton onClick={() => {}}>Back to dashboard</BackNavButton>
  </FixedWidthWrapper>
);

export default ParticipantProfile;
